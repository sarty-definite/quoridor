# Quoridor Frontend

Quick start:

1. Install dependencies: `npm install`
2. Run dev server: `npm run dev`

The frontend uses the local `core` package via a file dependency. Run `npm install` in the root `core` folder first if needed.

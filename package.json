{
  "name": "quoridor-monorepo",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:core": "cd core && npm ci && npm run build",
    "build:backend": "cd backend && npm ci && npm run build",
    "build:frontend": "cd frontend && npm ci && npm run build",
    "build_backend": "npm run build:core && npm run build:backend",
    "build_frontend": "npm run build:core && npm run build:frontend",
    "start": "cd backend && npm start"
  }
}
